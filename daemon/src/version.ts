/**
 * 버전 — **단일 출처.**
 *
 * 전에는 같은 문자열이 main.ts·protocol.ts·package.json·테스트 네 군데에 흩어져 있었다.
 * 버전을 올릴 때마다 전부 찾아 고쳐야 했고, 하나라도 놓치면 MCP 서버가 다른 버전을
 * 보고하거나 설치기의 원본 검증이 엉뚱한 값을 기대하게 된다.
 *
 * 이 파일만 고치면 코드 전체가 따라온다. `package.json` 은 별도 선언이 필요하므로
 * 그 둘이 어긋나면 테스트가 잡는다.
 *
 * 훅 시작 시간이 계약이라 이 모듈은 상수 하나만 둔다 — 무엇도 import 하지 않는다.
 */
export const VERSION = "2.0.1";
