{
  "name": "autoharness-daemon",
  "version": "2.0.0-dev",
  "private": true,
  "description": "AutoHarness v2 — 상주 데몬(스케줄러·웹·콘솔) + MCP + 훅을 하나의 EXE 로 담는다",
  "type": "module",
  "module": "src/main.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun run scripts/build.ts",
    "bench:startup": "bun run scripts/bench-startup.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
